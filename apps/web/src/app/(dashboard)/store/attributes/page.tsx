import { AttributesView } from "@/modules/store/attributes/AttributesView";

export const metadata = { title: "Product Attributes — Store" };

export default function AttributesPage() {
  return <AttributesView />;
}
