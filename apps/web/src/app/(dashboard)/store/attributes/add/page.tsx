import { AddAttributeView } from "@/modules/store/attributes/AddAttributeView";

export const metadata = { title: "Add Attribute — Store" };

export default function AddAttributePage() {
  return <AddAttributeView />;
}
