import { AddCustomerGroupView } from "@/modules/store/customer-groups/AddCustomerGroupView";

export const metadata = { title: "Add Customer Group — Store" };

export default function AddCustomerGroupPage() {
  return <AddCustomerGroupView />;
}
