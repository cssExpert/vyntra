{
  "name": "vyntra",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "workspaces": {
    "packages": [
      "apps/*",
      "libs/*"
    ]
  },
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "dev:web": "pnpm --filter @vyntra/web dev",
    "dev:app": "next dev",
    "dev:api": "pnpm --filter @vyntra/api dev",
    "build": "pnpm -r build",
    "build:types": "pnpm --filter @vyntra/types build",
    "build:web": "pnpm --filter @vyntra/web build",
    "build:api": "pnpm --filter @vyntra/api build",
    "lint": "pnpm -r lint",
    "type-check": "pnpm -r type-check",
    "db:up": "docker compose -f infrastructure/docker-compose.yml up -d",
    "db:down": "docker compose -f infrastructure/docker-compose.yml down",
    "db:migrate": "pnpm --filter @vyntra/api prisma migrate dev",
    "db:seed": "pnpm --filter @vyntra/api prisma:seed",
    "db:studio": "pnpm --filter @vyntra/api prisma studio"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "typescript": "^5.7.2"
  }
}
